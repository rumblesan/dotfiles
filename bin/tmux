#!/bin/bash


die() {
    echo "$*"
    exit 1
}


main () {

    # Start tmux in every new shell
    if [ -z "$TMUX" ]; then
        export TERM='xterm-256color'
        exec tmux
    else
        die "Not nesting sessions"
    fi

}

main "$@"

