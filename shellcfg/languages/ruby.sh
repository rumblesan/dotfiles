# ----------------------------------------------------------------------
# RUBY
# ----------------------------------------------------------------------
# Ruby specific config

if [ -n "$(command -v rbenv)" ]; then
  eval "$(rbenv init -)"
fi
